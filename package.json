{
  "name": "stack-wire-site",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "narrate": "node scripts/generate-audio.mjs",
    "build": "node scripts/build-site.mjs",
    "dev": "npm run build && npx serve dist"
  }
}
